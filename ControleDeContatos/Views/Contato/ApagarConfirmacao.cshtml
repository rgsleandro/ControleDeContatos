@model ContatoModel

@{
    ViewData["Title"] = "Confirmação Apagar Contato";
}

<div class="text-center">
    <h3>Você deseja apagar o contado @Model.Nome permanentemente? </h3>
</div>
<a class="btn btn-danger btn-block" role="button" asp-controller="Contato" asp-action="Index">Cancelar</a>
<a class="btn btn-primary btn-block" asp-action="Apagar" asp-controller="Contato" asp-route-id="@Model.Id" role="button">Confirmar</a>